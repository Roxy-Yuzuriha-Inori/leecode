# 题目
给定一个数组 nums，编写一个函数将所有 0 移动到数组的末尾，同时保持非零元素的相对顺序。
请注意 ，必须在不复制数组的情况下原地对数组进行操作。
- 示例 1：
  - 输入: nums = [0,1,0,3,12]
  - 输出: [1,3,12,0,0]
- 示例 2:
  - 输入: nums = [0]
  - 输出: [0]
# 自解
- 不满足是同一个数组
- 复杂度为O(n)
```java
    class Solution {
        public void moveZeroes(int[] nums) {
            int[] a=new int [nums.length];
	         int n = 0;
        for(int i = 0;i < nums.length;i ++){
        if(nums[i]!=0){
        a[n]=nums[i];
	n++;	
    }

    }
    System.out.println(Arrays.toString(a));
        }
    }
```
# 原解
```java
////双指针，a指针不动，b指针++，当b指针遇到不为0的数，ab互换值，并且a++
class Solution {
	public void moveZeroes(int[] nums) {
		if(nums==null) {
			return;
		}
		//两个指针i和j
		int j = 0;
		for(int i=0;i<nums.length;i++) {
			//当前元素!=0，就把其交换到左边，等于0的交换到右边
			if(nums[i]!=0) {
				int tmp = nums[i];
				nums[i] = nums[j];
				nums[j++] = tmp;
			}
		}
	}
}	    
```
# 知识点
- java不能用非0数字代表真
- Arrays.toString(a) 输出数组代表值  直接a输出数组地址
- Arrays.asList() 用于将数组转换为 固定大小的 List，不能增删元素，可以修改元素
```java
String[] arr = {"A", "B", "C"};
List<String> list = Arrays.asList(arr);

System.out.println(list); // [A, B, C]
```
# 总结
数组排序问题，双指针<br>

